import os
import secrets

STRIPE_SECRET = os.environ.get("STRIPE_SECRET", "")
BASE_URL = os.environ.get("BASE_URL", "https://sebbi.pro").rstrip("/")
PORT = int(os.environ.get("PORT", 8080))
ADMIN_KEY = os.environ.get("ADMIN_KEY", secrets.token_hex(32))
DB_PATH = os.environ.get("DB_PATH", "aileash.db")
FREE_TIER_LIMIT = 1000
VERSION = "2.5.0"
PRODUCT = "AILeash"

COUNTRY_RISK = {
    "UK": 0.05, "US": 0.05, "DE": 0.05, "FR": 0.05,
    "CA": 0.05, "AU": 0.05, "JP": 0.10, "BR": 0.25,
    "IN": 0.20, "CN": 0.40, "RU": 0.70, "KP": 0.90,
    "IR": 0.80, "BY": 0.70, "SY": 0.80, "NG": 0.50,
}
DEFAULT_COUNTRY_RISK = 0.35
